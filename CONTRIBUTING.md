# 기여하기

DevTrail 은 **남의 볼트를 건드리는 도구**다. 그래서 기여의 기준이
"동작하는가"가 아니라 **"남의 노트를 잃게 하지 않는가"** 다.

먼저 [아키텍처 문서](docs/ARCHITECTURE.md)를 읽어주세요. 계층과 규칙 10가지가
거기 있고, 아래는 그 위에서의 실무 절차입니다.

> **언어**: 이 저장소는 한국어로 개발합니다. 이슈·PR·주석·문서 모두 한국어를
> 씁니다. 코드 식별자와 커밋 제목의 타입(`feat:` 등)만 영어입니다.
> 영어로 이슈를 여셔도 됩니다 — 답은 한국어로 갑니다.

---

## 준비

```bash
git clone https://github.com/p-changki/devtrail.git
cd devtrail
./tests/run.sh
```

필요한 것: `bash` · `jq` · `python3` · `git`.
메뉴바 앱까지 고치려면 Xcode Command Line Tools 가 필요합니다.

**개발용 체크아웃에서는 `devtrail update` 가 갱신하지 않습니다.** 로컬이
원격보다 앞서 있으면 멈추고 알려줍니다 — 미푸시 커밋을 지우지 않기 위해서입니다.

### 자기 볼트를 망가뜨리지 않고 시험하기

```bash
export DEVTRAIL_HOME=/tmp/dt-test          # 설정·저널이 여기로 격리됩니다
export DEVTRAIL_CONFIG=/tmp/dt-test/c.json
mkdir -p /tmp/dt-test /tmp/dt-vault

./bin/devtrail augment           # dry-run — 무엇을 만들지만 보여줍니다
./bin/devtrail augment --apply
./bin/devtrail undo              # 되돌릴 수 있는지 확인
```

`tests/lib/harness.sh` 의 `t_vault` 가 테스트에서 같은 일을 합니다.

---

## 브랜치

| 브랜치 | 무엇 |
|---|---|
| `main` | **프로덕션.** `install.sh` 가 여기서 받아갑니다 |
| `dev` | 개발. PR 은 기본적으로 여기로 |

`main` 은 보호돼 있습니다 — **직접 밀 수 없습니다.** 관리자도 예외가 아니고,
force push 와 브랜치 삭제도 막혀 있습니다. 릴리스도 PR 로 들어갑니다
(절차는 [CHANGELOG](CHANGELOG.md) 머리에 있습니다).

```bash
git switch dev && git pull
git switch -c fix/undo-empty-parent
```

브랜치 이름: `feat/` · `fix/` · `docs/` · `refactor/` · `test/` · `ci/`.

---

## 커밋

```
<type>: <무엇을 했는가>

<왜. 무엇이 문제였는가. 어떻게 확인했는가>
```

타입: `feat` · `fix` · `refactor` · `docs` · `test` · `chore` · `perf` · `ci` · `build`.

본문에 **왜**를 씁니다. "무엇을"은 diff 를 보면 압니다. 왜 그렇게 했는지,
어떤 사고가 있었는지는 diff 에 없습니다.

```
fix: undo 가 비어 있지 않은 폴더를 조용히 넘겼다

"삭제"라고 말해놓고 남겨두면 사용자는 지워진 줄 안다.
rmdir 실패를 반드시 알리게 했다.
```

---

## 검사

**커밋 전에 이걸 돌립니다.**

```bash
./tests/run.sh          # 전부 (swift 빌드 포함)
./tests/run.sh fast     # swift 빼고 — 평소엔 이걸로 충분합니다
```

CI 도 같은 스크립트를 부릅니다. "내 머신에서는 됐는데"가 생기지 않게 하려는
것입니다.

### 이 저장소 고유의 함정

**1. macOS 기본 bash 는 3.2 입니다.**

```bash
"$n개"     # ✗ 3.2 가 '개'의 첫 바이트를 변수명에 흡수 → unbound variable
"${n}개"   # ✓
```

세 번 사고가 났고, 세 번 다 **문제를 찾았을 때만** 죽었습니다(요약 분기,
에러 분기). 그래서 평소 테스트로는 안 잡혔습니다.

`mapfile` · `declare -A` · `${var^^}` 도 쓸 수 없습니다.

**이 함정은 Linux 의 bash 5 에서 재현되지 않습니다.** CI 의 동작 테스트가
macOS 에서만 도는 이유입니다.

**2. 볼트 경로를 박지 마세요.**

`devtrail path` / `dt_dir()` 가 단일 출처입니다. 사용자가 폴더 이름을 바꾸면
박아둔 경로는 전부 깨집니다.

**3. `jq //` 를 boolean 이나 빈 값에 쓰지 마세요.**

`false // "기본값"` 은 `"기본값"` 을 냅니다. 키의 존재를 물으려면 `has()`.

---

## 테스트

### 언제 쓰는가

| 바꾼 것 | 요구 |
|---|---|
| 문구 · 주석 · 스타일 | 검사 통과만 |
| 기능 · 버그 수정 | 그 **실패 조건을 재현하는** 테스트 1개 이상 |
| 계약 (설정 스키마 · 경로 · 프로필) | 왕복 테스트 필수 |
| 사용자 파일을 지우거나 덮어쓰는 것 | **보존 테스트 필수** |

마지막 항목이 이 저장소에서 가장 중요합니다. `tests/test-undo.sh` 의
「사용자 노트 보존」이 본보기입니다.

### 테스트가 존재한다 ≠ 테스트가 무언가를 지킨다

차단성 결함을 고쳤으면 **변이를 주입해 검사가 빨간불이 되는지 확인하세요.**

```bash
# 예: undo 가 사용자 노트를 지키는지
#   lib/journal.sh 의 rmdir 를 rm -rf 로 바꾼 뒤
./tests/test-undo.sh     # → ✗ 내 노트가 살아 있다   ← 이게 나와야 합니다
#   되돌린다
```

변이가 **실제로 코드를 바꿨는지 먼저 확인하세요.** 정규식이 대상을 못 찾아
아무것도 안 바꿨는데 "생존"으로 보인 사례가 있었습니다.

PR 본문에 무엇을 주입했고 어떤 검사가 빨개졌는지 적어주시면 리뷰가 빨라집니다.

---

## 새로 만들 때

| 만들려는 것 | 위치 | 진입점 |
|---|---|---|
| CLI 명령 | `lib/<이름>cmd.sh` | `<이름>_cmd()` |
| Obsidian 설정 병합 | `lib/merge/<대상>.sh` | `_ob_<대상>()` |
| init 단계 | `lib/init/<단계>.sh` | `_init_<단계>()` |
| 설정 스키마 변경 | `lib/migrations/NNN-<이름>.sh` | `_mg_NNN()` + `_mg_NNN_why` |
| 노트·JSON 생성기 | `lib/gen/<이름>.py` | stdin/argv → stdout |
| 폴더 구조 · 기본값 | `preset/` | (데이터) |
| AI 스킬 | `skills/<이름>/SKILL.md` | (데이터) |

새 명령은 `bin/devtrail` 의 `case` 와 `usage()` **두 곳**을 고칩니다.

**파일이 400줄을 넘으면** 분리를 검토하세요. 600줄은 검사가 막습니다.
길다는 것은 대개 두 가지를 하고 있다는 뜻입니다.

---

## 사용자 파일을 건드리는 코드

```bash
jr_backup "$file" >/dev/null || die "백업 실패 — 원본을 건드리지 않습니다"
# ... 이제 쓴다

jr_mkdir "$dir"      # mkdir -p 대신. 새로 생긴 모든 단계를 기록합니다
jr_created "$file"   # 파일을 만들었으면
```

- 백업이 실패하면 **원본을 건드리지 않고 멈춥니다.**
- `mkdir -p` 를 직접 부르지 마세요 — 중간 단계가 기록되지 않아 되돌린 뒤
  빈 껍데기가 남습니다.
- 볼트를 바꾸는 명령은 **기본이 dry-run** 입니다. `--apply` 가 있어야 씁니다.

---

## PR

1. `dev` 로 PR 을 엽니다
2. PR 템플릿을 채웁니다 — 특히 **어떻게 검증했는가**
3. CI 3잡이 초록불이어야 합니다 (`메뉴바 앱 빌드` 는 태그를 밀 때만 돕니다)
4. 리뷰 후 머지

`main` 머지와 배포는 저장소 소유자가 합니다.

### 리뷰에서 보는 것

1. 사용자 요구를 지켰는가
2. 계약을 깼는가 — 설정 스키마 · 경로 · 프로필
3. 실제 사용자 회귀가 있는가
4. 테스트가 **핵심 위험**을 재는가

주석 문구나 명명 취향은 지적하지 않습니다 — 동작에 대해 거짓을 말하는
경우만 예외입니다.

---

## 버그를 발견했다면

이 도구는 **볼트 상태에 따라 동작이 갈립니다.** 그래서 재현에 정보가
필요합니다:

```bash
devtrail version
devtrail doctor
devtrail scan
```

⚠️ **붙여넣기 전에 확인하세요.** `scan` 출력에는 볼트 경로와 폴더 이름이
들어갑니다. 개인적인 이름이 있으면 지우고 올려주세요.

이슈 템플릿이 이 항목들을 물어봅니다.

---

## 행동

한국어든 영어든, 서툴든 아니든 편하게 여세요. 질문은 이슈로 충분합니다.

기여해주셔서 고맙습니다.
